# Agape Identity Protocol v2.0
Deterministic 16,384-node resonant identity primitive — 100% reproducible worldwide.
MIT licensed · Zero external entropy · Pure mathematics.

python aip_cli.py --generate          # canonical eternal identity
python aip_cli.py --generate --vault  # + encrypted seed vault
